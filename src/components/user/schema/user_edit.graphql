query User($id: ID) {
  user(id: $id) {
    _id
    firstName
    lastName
    email
    sellerCode
    phone
    business {
      _id
      name
      country
      dbName
    }
    roles {
      _id
      name
    }
    mode
    status
    lang
  }
}
